---
date: 2026-07-12
---

# 2026-07-12 每日精選

## boris_cherny · X

**原文網址：** [https://x.com/ClaudeDevs/status/2075635283211772279](https://x.com/ClaudeDevs/status/2075635283211772279)

**原文：**
> Claude Code on desktop now has an in-app browser.
>
> Claude can pull up docs, designs, or any other site. It can read, click through, and interact the same way it does with your local dev servers.
>
> It's sandboxed and configurable: you choose whether sessions persist.

**繁中改寫：**
Claude Code 桌面版現在內建瀏覽器了。Claude 可以直接打開文件、設計稿，或任何網站，讀取內容、點擊互動的方式跟操作你本機的 dev server 一樣。這個瀏覽器是沙盒化且可設定的，你可以自己決定 session 要不要保留。

**核心概念（簡單說）：**
這則貼文（Boris Cherny 轉發自 ClaudeDevs）宣布 Claude Code 桌面版新增了一個內嵌（in-app）瀏覽器功能。過去 Claude Code 主要透過終端機操作程式碼，若要驗證 UI 變化，開發者得自己切到瀏覽器手動檢查。現在 Claude 能在應用程式內直接開啟瀏覽器分頁，像人類開發者一樣瀏覽文件、設計稿或任何網站，並能讀取畫面內容、點擊連結、與頁面互動，這跟 Claude 操作本機 dev server（例如 localhost:3000）的方式是相同的技術基礎，只是把瀏覽範圍擴大到任意網站。沙盒化與可設定的 session 持久性，代表你可以控制 Claude 瀏覽器的隔離程度與登入狀態是否保留，兼顧安全性與便利性。

**前端工程師實際應用：**
- **跟你工作的關聯：** 這功能直接補齊了 Claude Code 過去缺乏「視覺驗證」能力的痛點——以前你得自己開瀏覽器截圖給 Claude 看，現在它能自己看、自己驗證。
- **具體場景：** 你要 Claude 依照 Figma 設計稿刻版面時，過去得手動貼截圖說明差異；現在可以直接請它「打開這個 Figma 連結，跟目前畫面比對」，Claude 會自己開瀏覽器查看設計稿再回來改程式碼。
- **這週可以嘗試：** 升級 Claude Code 桌面版後，找一個正在刻的頁面，請 Claude 直接開啟你部署在 staging 或本機的頁面 URL，用「這個按鈕的 hover 效果跟設計稿不符，幫我抓出差異」這類指令，實測內建瀏覽器的效果。

---

## trq212 · X

**原文網址：** [https://x.com/ClaudeDevs/status/2075635283211772279](https://x.com/ClaudeDevs/status/2075635283211772279)

**原文：**
> Claude Code on desktop now has an in-app browser.
>
> Claude can pull up docs, designs, or any other site. It can read, click through, and interact the same way it does with your local dev servers.
>
> It's sandboxed and configurable: you choose whether sessions persist.

**繁中改寫：**
Claude Code 桌面版現在內建瀏覽器了。Claude 可以直接打開文件、設計稿，或任何網站，讀取內容、點擊互動的方式跟操作你本機的 dev server 一樣。這個瀏覽器是沙盒化且可設定的，你可以自己決定 session 要不要保留。

**核心概念（簡單說）：**
Thariq 轉發了同一則 ClaudeDevs 公告（跟上方 boris_cherny 轉發的是同一則貼文），說明 Claude Code 桌面版新增內嵌瀏覽器。這個功能讓 Claude 可以像人類開發者一樣，在應用程式內直接開啟瀏覽器查看文件、設計稿或任何網站，並能讀取、點擊、互動，補上過去只能操作本機 dev server 的限制。

**前端工程師實際應用：**
- **跟你工作的關聯：** 內建瀏覽器讓 Claude 從「只能看你貼的截圖」進化到「自己主動去看」，減少你在對話中反覆貼圖說明的來回成本。
- **具體場景：** Code review 時想請 Claude 對照 PR 預覽網址（Vercel preview link）檢查是否有 UI regression，現在可以直接丟連結請它自己打開比對。
- **這週可以嘗試：** 挑一個目前有 PR 預覽連結的專案，請 Claude 開啟該連結並檢查特定頁面在不同裝置寬度下是否有版面跑掉的問題。

---

**原文網址：** [https://x.com/RLanceMartin/status/2075641284635799865](https://x.com/RLanceMartin/status/2075641284635799865)

**原文：**
> Cost effective harnesses with Fable
>
> There is a lot of interest in cost effective use of Fable 5. Agent harnesses will get better at knowing when to use frontier intelligence. I wanted to share some tests I've run to better understand when and how to use Fable 5.
>
> **The task shape**
>
> Many tasks have asymmetry in the intelligence needed across their tokens. Harnesses can recognize this and pick when to use Fable 5. A few patterns have emerged, but we'll likely see more over time:
> - Use Fable 5 as an orchestrator that delegates to lower cost workers.
> - Use Fable 5 as an advisor that lower cost executors ask for advice.
> - Use Fable 5 as a verifier to check work (e.g., in a /goal or Outcomes loop).
>
> For example, @mitchellh called out an orchestrator-verifier approach: "I'm having a lot success using Fable xhigh as a planner/architect, using GPT 5.5 xhigh (subscription) as a coder, then Fable xhigh again as a judge. At API pricing, planning+judge costs are in the ~few dollar range compared to typical $50+ full round trips."
>
> I explored this on Parameter Golf, an ML engineering challenge similar to @karpathy's autoresearch: let an agent edit training code, launch training, see results, and decide what experiment to run next.
>
> The goal is to train the best model that fits in a 16MB artifact in < 10 minutes on 8xH100s. I previously showed that Fable 5 is strong at this task. So, I wanted to see if I could use Fable 5 only for experimental design and Sonnet 5 as a worker to absorb the implementation tokens.
>
> I set this up with Claude Managed Agents with access to a @modal self-hosted 8xH100 sandbox and a Sonnet 5 executor that can call Fable 5. I instructed Sonnet 5 to consult Fable 5 as an advisor on the initial plan and later at 2 checkpoints during the 20 experiments.
>
> The results below show validation loss (bits per byte) across 3 configurations (where lower is better): Fable 5 and Sonnet 5 together got ~90% of Fable-5-solo's improvement at ~34% of the token cost.
>
> The upfront advising step was not the primary benefit. Fable 5's initial ranking was anti-correlated with what worked.
>
> The value came from the advisory checkpoints. Sonnet 5 tends to get caught hill-climbing on marginal gains with no tendency to step back and re-rank. Fable's checkpoints provide steering and re-prioritization.
>
> The distribution of judgment mattered in this case: upfront planning wasn't sufficient, but sprinkling Fable 5 as an advisor across the task at fixed points helped steer it in more promising directions.
>
> In hindsight this matches the task's shape: this type of experimentation is exploratory and each result reshapes what's worth trying next. Judgment needs to be scattered across it rather than front-loaded.
>
> **The cost of delegation**
>
> Even if a task has intelligence asymmetry that the harness can exploit, it doesn't always pay to offload it. Sometimes we just do things ourselves because there's a coordination cost involved in delegation.
>
> @brada and I saw this when testing Fable 5 with BrowseComp, an eval for multi-constraint web search. It's a good task shape for Fable 5 to plan and delegate to Sonnet 5 workers that search, open pages, and cross-reference until the constraints pin a unique answer.
>
> On BrowseComp200, an easy subset with ~0.37M tokens of reading per problem, Fable 5 alone was cheaper than mixing Fable 5 with Sonnet 5. Orchestration added a 60% markup for no benefit in performance.
>
> But on the full BrowseComp eval set (~31M tokens of reading per problem), orchestration paid off. Fable 5 orchestrator with Sonnet 5 workers landed at 96% of the score at 46% of the cost.
>
> The token cost arbitrage gained by delegation to workers needs to offset the coordination cost. In this case, coordination cost has a few components:
> - Boundary duplication - every token that crosses between models is billed at least twice: the lead writes a brief, the worker reads it; the worker writes a report, the lead reads it.
> - Fan-out overlap – in many harnesses, workers don't communicate and many partially overlap in their research. @walden_yan wrote a nice article on this problem last year.
>
> This means the cost benefit of cheap workers has to offset a coordination cost that is roughly fixed per handoff. In this case, the worker benefit scales with the tokens each worker absorbs.
>
> **Cost effective harnesses**
>
> Here are the types of guidance that I've been giving Fable 5 when writing cost effective harnesses for various tasks; Fable is effective at understanding how and when to utilize its own intelligence with some of this guidance:
>
> 1. Examine the task shape. Assess the intelligence needed across the task. Judgement scattered across the task, as we saw in Parameter Golf, can benefit from a cheap executor and a Fable 5 advisor. Judgement upfront or to review work can benefit from a Fable 5 orchestrator or verifier.
> 2. Use delegation heuristics. Sometimes we can provide Claude with priors for worker delegation. @theo has an example where various models are ranked according to "taste" and "intelligence"; these can help the harness decide when to incorporate each one.
> 3. Assess the cost of coordination. Delegation comes with a cost. As I saw with BrowseComp, make sure that you are delegating a large enough volume of tokens to offset the coordination cost. Because Fable 5 can be more token efficient than a model with a lower $/token, the benefit of delegation should be carefully considered.
> 4. Ensure prompt caching. Models maintain their own prompt caches, and getting this wrong is an easy way for delegation costs to blow up. As @cognition calls out here, sub-agents should maintain a prompt cache across calls. Route calls to the same worker so its cache accumulates, rather than spawning a fresh worker per request and re-paying the context write every time. In my experiments, Claude Managed Agents has sub-agents that supported this natively, but I've seen cases where a low prompt cache hit rate offsets the cost benefit of using a lower $/token worker.
>
> As @trq212 shared here, Claude can write its own harness on the fly based upon the task. Some of the considerations in this article can help Claude write cost effective harnesses that selectively apply frontier intelligence.

**繁中改寫：**
Lance Martin（分享者為 trq212 轉發）發表了一篇長文，探討怎麼用 Fable 5 打造「省成本」的 agent harness。核心觀察是：很多任務裡，各個 token 所需要的智慧程度並不平均，harness 應該要能判斷什麼時候值得動用最頂級的模型。他歸納出三種常見用法：讓 Fable 5 當「orchestrator」負責分派任務給便宜的 worker、當「advisor」讓便宜模型隨時諮詢、或當「verifier」在最後把關檢查結果。他在自己的 ML 實驗（Parameter Golf）中實測，用 Sonnet 5 當執行者、只在關鍵時間點請 Fable 5 給建議，最終達到 Fable 5 單獨全程執行 90% 的效果，但 token 成本只要 34%。他也用 BrowseComp 這個網頁搜尋測試證明：分派任務給便宜模型不是永遠划算，資料量小的時候，多一層協調反而多花 60% 成本卻沒有效益提升；資料量大時，協調的成本才會被攤平回本（46% 成本拿到 96% 分數）。最後他整理出四點寫 cost-effective harness 的心法：判斷任務形狀、善用委派的經驗法則、評估協調成本、以及確保 prompt caching 有效運作。

**核心概念（簡單說）：**
這篇文章討論的是多模型（multi-agent / multi-model）協作架構下的成本優化問題，可以類比成「主管跟助理怎麼分工最划算」。如果每件事都找最貴、最強的模型（主管）處理，成本會爆炸；但如果全部丟給便宜模型（助理），品質可能不夠。作者發現關鍵不是「要不要分工」，而是「分工的協調成本能不能被省下來的錢覆蓋」。當每次交接（模型之間互相寫報告、讀報告）都要花費 token，如果任務資料量小、交接次數多，協調成本會蓋過省下的錢；但資料量夠大時，讓便宜模型做大量苦工、只在關鍵點請頂級模型把關或指導，就能大幅省錢又不太犧牲品質。這跟前端工程師熟悉的「什麼工作該交給資深工程師 review，什麼可以放心讓 junior 處理」的判斷邏輯本質上是同一件事。

**前端工程師實際應用：**
- **跟你工作的關聯：** 如果你在專案裡用多個 agent 或多階段 AI 流程處理任務（例如先用便宜模型產生程式碼草稿，再用強模型 review），這篇文章的成本模型可以直接套用來評估你的架構划不划算。
- **具體場景：** 假設你在寫一個「AI code review 機器人」，每個 PR 都先讓便宜模型掃過一輪抓明顯問題，再由頂級模型做最終把關；這正是文章講的 verifier 模式，但要注意如果 PR diff 很小，兩層模型的溝通成本可能比直接用一個強模型審查還貴。
- **這週可以嘗試：** 檢視你手上任何用到多個 AI 呼叫串接的流程（例如 CI 中的 AI 檢查步驟），估算一下每次模型間交接（傳輸 prompt、回應）花掉多少 token，跟直接用單一頂級模型處理相比是否真的省錢，必要時砍掉不必要的中間層。

---

## trq212 (Thariq) · Thread Reader App

（今日無更新）

---

## claudeai · Anthropic Blog

（今日無更新）

---

## claudeai · X

（今日無更新）

---

## Claude Code · Changelog

**版本號 / 日期：** 2.1.207 / July 11, 2026

**變更項目：**
- Auto mode is now available without `CLAUDE_CODE_ENABLE_AUTO_MODE` opt-in on Bedrock, Vertex AI, and Foundry; disable via `disableAutoMode` in settings（Bedrock、Vertex AI、Foundry 上的 Auto mode 現在預設可用，不需要再手動開啟 opt-in 旗標，若要關閉可透過設定中的 `disableAutoMode`）
- Fixed the terminal freezing and keystrokes lagging while streaming responses containing very long lists, tables, paragraphs, or code blocks（修正回應內容含超長列表、表格、段落或程式碼區塊時，終端機會凍結、打字延遲的問題）
- Fixed remote managed settings from a non-interactive run (`claude -p`, the SDK) being permanently recorded as consented without ever showing the security consent dialog（修正非互動模式執行時，遠端受管設定會被永久記錄為「已同意」卻從未實際顯示過安全同意對話框的問題）
- Fixed spurious prompt-injection warnings triggered by benign system-generated conversation updates（修正系統自動產生的無害對話更新，會誤觸發 prompt-injection 警告的問題）
- Fixed the auto-updater overwriting a custom launcher script or symlink at `~/.local/bin/claude` on every release; `/doctor` now reports an externally managed launcher（修正自動更新程式每次發版都會覆蓋 `~/.local/bin/claude` 的自訂啟動腳本或 symlink 的問題；`/doctor` 現在會回報有外部管理的啟動程式）
- Fixed compound commands with `cd` prompting for permission when the only output redirect was to `/dev/null`（修正含 `cd` 的複合指令，即使唯一的輸出重導向只是 `/dev/null`，仍會跳出權限詢問的問題）
- Fixed the transcript jumping above the start of the answer when a response finishes streaming（修正回應串流結束時，畫面會跳到答案開頭上方的問題）
- Fixed `extensions.worktreeConfig` being left in the repo's `.git/config` (breaking go-git tools like tea) after the last `worktree.sparsePaths` worktree was removed（修正移除最後一個 `worktree.sparsePaths` worktree 後，`extensions.worktreeConfig` 殘留在 `.git/config` 中、導致 tea 等 go-git 工具異常的問題）
- Fixed malformed bracket patterns in rules globs, skill paths, `.ignore`, and `.worktreeinclude` breaking file reads, file suggestions, and worktree creation（修正 rules glob、skill 路徑、`.ignore`、`.worktreeinclude` 中格式錯誤的括號樣式，會導致檔案讀取、檔案建議、worktree 建立失敗的問題）
- Fixed a crash loop in agent teams where a malformed teammate mailbox message caused repeated errors every second until the mailbox file was manually deleted（修正 agent teams 中格式錯誤的隊友信箱訊息會導致每秒重複噴錯、造成當機迴圈，需手動刪除信箱檔案才能解除的問題）
- Fixed background sessions auto-named by accepting a plan not showing that name on their agent-view row（修正接受計畫後自動命名的背景 session，在 agent 檢視列表中不會顯示該名稱的問題）
- Fixed background sessions that entered a git worktree resuming blank after a cold reopen from the agent list（修正進入 git worktree 的背景 session，從 agent 列表冷啟動重新開啟後畫面空白的問題）
- Fixed Remote Control task status updates being lost when the connection recovered from a network interruption or credential refresh（修正網路中斷或憑證刷新後連線恢復時，Remote Control 的任務狀態更新會遺失的問題）
- Fixed Remote Control sessions hosted by the desktop app not showing background agent and workflow progress on mobile and web（修正桌面版託管的 Remote Control session，在手機版與網頁版看不到背景 agent 與 workflow 進度的問題）
- Fixed Deep research runs labeling every Fetch-phase agent "unknown" — chips now show the source hostname（修正 Deep research 執行時，所有 Fetch 階段的 agent 都被標成「unknown」的問題，現在標籤會顯示來源網域）
- Fixed Bedrock repeatedly requesting fresh AWS SSO credentials from IAM Identity Center on every API request（修正 Bedrock 每次 API 請求都會重複跟 IAM Identity Center 要求全新 AWS SSO 憑證的問題）
- Improved agent view: pasting the same text again now expands the collapsed `[Pasted text #N]` placeholder instead of adding a second one（改善 agent 檢視：再次貼上相同文字時，會展開已摺疊的 `[Pasted text #N]` 佔位符，而不是新增第二個）
- Improved agent view: blocked session peeks now lead with the question and show a worded staleness clock (`waiting 3m`) instead of the same timestamp twice（改善 agent 檢視：被阻擋的 session 預覽現在會優先顯示問題內容，並以文字化的等待時間（如 `waiting 3m`）取代重複顯示同一個時間戳記）
- Changed Bedrock, Vertex, and Claude Platform on AWS to default to Claude Opus 4.8（Bedrock、Vertex 與 AWS 上的 Claude Platform 預設模型改為 Claude Opus 4.8）
- Changed auto mode to no longer read `autoMode` from `.claude/settings.local.json` (repo-resident); use `~/.claude/settings.json` instead（Auto mode 不再讀取存放於 repo 內的 `.claude/settings.local.json` 的 `autoMode` 設定，改為讀取 `~/.claude/settings.json`）
- Fixed an indefinite hang on Windows when AWS credential resolution stalls (e.g. a stuck `credential_process`): the 60-second stall guard now fires instead of waiting forever（修正 Windows 上 AWS 憑證解析卡住時（例如 `credential_process` 卡死）會無限期掛起的問題，現在 60 秒逾時保護會正確觸發）
- Plugin hooks/monitors/MCP headersHelper: `${user_config.*}` in shell-form commands is now rejected (shell-injection fix). Hooks: use exec form (args array) or `$CLAUDE_PLUGIN_OPTION_<KEY>`; monitors and headersHelper: read the value inside the script（安全性修正：plugin hooks／monitors／MCP headersHelper 中 shell 形式指令內的 `${user_config.*}` 現在會被拒絕，避免 shell injection；hooks 請改用 exec 形式（args 陣列）或 `$CLAUDE_PLUGIN_OPTION_<KEY>`，monitors 與 headersHelper 請在腳本內部讀取該值）
- Plugin option values (`pluginConfigs`) are no longer read from project-level `.claude/settings.json`; only user, `--settings`, and managed settings are honored（Plugin 選項值 `pluginConfigs` 不再從專案層級的 `.claude/settings.json` 讀取，只認使用者層級、`--settings` 與受管設定）
- Fixed `/usage-credits` amount inputs silently stripping malformed values (e.g. a pasted timestamp) to digits; malformed amounts are now rejected with an error, and amounts over $1,000 require a typed confirmation（修正 `/usage-credits` 金額輸入時，格式錯誤的值（例如誤貼時間戳記）會被靜默截取成數字的問題；現在格式錯誤會直接報錯拒絕，超過 $1,000 的金額則需要手動輸入確認）

**核心概念（簡單說）：**
這次更新（2.1.207）的重點是三個方向：一是 Auto mode 在企業雲端環境（Bedrock、Vertex AI、Foundry）上從「需手動開啟」變成「預設可用」，降低導入門檻；二是大量修正終端機渲染、Remote Control 連線同步、agent teams 錯誤處理等穩定性問題，尤其是長內容串流造成的終端機凍結、以及背景 session 在跨裝置同步上的多個 bug；三是安全性強化，堵住了 plugin hooks 中 shell 指令可能被注入的漏洞，並收緊了 `pluginConfigs` 的讀取來源與 `/usage-credits` 金額輸入的驗證。整體來說是一次以穩定性與安全性收斂為主的維護性版本。

**前端工程師實際應用：**
- **跟你工作的關聯：** 如果你的團隊有用 plugin hooks 或 monitors，這次的 shell-injection 修正是必看項目，因為舊寫法（在 shell-form 指令裡直接內插 `${user_config.*}`）現在會被拒絕，需要改寫。
- **具體場景：** 假設你在 `.claude/settings.local.json` 裡設定了 `autoMode`，這次更新後會不再生效，因為讀取來源改成 `~/.claude/settings.json`，若團隊依賴 repo 內的自動模式設定同步給所有人，這個行為變更會讓設定「突然失效」而不易察覺。
- **這週可以嘗試：** 檢查團隊 repo 裡是否有 plugin hooks 用了 shell-form 加 `${user_config.*}` 的寫法，若有就照 changelog 建議改成 exec 形式（args 陣列）或改用 `$CLAUDE_PLUGIN_OPTION_<KEY>` 環境變數，避免升級後功能悄悄失效。
